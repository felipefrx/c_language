#include <stdio.h>

int main(){

    int x = 10;
    int y = 2;

    printf("%d\n", x==y); // Return 0(False).

    return 0;
}

    /*
        == : Equal
        != : Different
        >  : Bigger
        <  : Smaller
        >= : Bigger or equal
        <= : Smaller or equal

        && : AND
        || : OR
        !  : NOT 
    */
